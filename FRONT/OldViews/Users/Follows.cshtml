@using Web.Domain.DTOs.UserAccount
@{
    UserDto user = Model[0];
    ICollection<UserViewDto> follows = Model[1];
}

<div>
    @foreach (var follow in follows)
    {
        <h3>
            @follow.NickName
        </h3>

        @if (follow.Avatar != null)
        {
            <img style='width:40px; height:30px;' src="data:image/jpeg;base64,@(Convert.ToBase64String(follow.Avatar))"/>
        }

        @* if (user.Email == User.Identity.Name) *@
        @* { *@
        @*     <form asp-area="" asp-controller="User" asp-action="DeleteFollow" method="post"> *@
        @*         <input type="hidden" id="userId" name="userId" value="@follow.Id"/> *@
        @*         <input type="submit" asp-controller="User" asp-action="DeleteFollow" value="Відстежується"/> *@
        @*     </form> *@
        @* } *@
    }
</div>