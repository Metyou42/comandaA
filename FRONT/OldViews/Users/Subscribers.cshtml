@using Web.Domain.DTOs.UserAccount
@{
    UserDto user = Model[0];
    ICollection<UserViewDto> subscribers = Model[1];
}

<div>
    @foreach (var subscriber in subscribers)
    {
        <h3>
            @subscriber.NickName
        </h3>

        @if (subscriber.Avatar != null)
        {
            <img style='width:40px; height:30px;' src="data:image/jpeg;base64,@(Convert.ToBase64String(subscriber.Avatar))"/>
        }

        @* if (user.Email == User.Identity.Name) *@
        @* { *@
        @*     <form asp-area="" asp-controller="User" asp-action="DeleteSubscriber" method="post"> *@
        @*         <input type="hidden" id="userId" name="userId" value="@subscriber.Id"/> *@
        @*         <input type="submit" asp-controller="User" asp-action="DeleteSubscriber" value="Видалити"/> *@
        @*     </form> *@
        @* } *@
    }
</div>